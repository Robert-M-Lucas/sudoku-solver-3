use crate::board::Possibilities;
use crate::solution::Solution;

pub fn solve_backtracking(solution: Solution) -> Option<Solution> {
    let base = Possibilities::from_solution(&solution);
    todo!()
}

pub fn recursively_attempt(board: Possibilities) -> Option<Possibilities> {

    todo!()
}